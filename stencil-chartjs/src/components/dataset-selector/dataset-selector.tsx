import { Component, Host, h } from '@stencil/core';

@Component({
  tag: 'dataset-selector',
  styleUrl: 'dataset-selector.css',
  shadow: true,
})
export class DatasetSelector {

  render() {
    return (
      <div>selezione dataset</div>
    );
  }

}
